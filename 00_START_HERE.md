# Blacksmith

Pre-boot gatekeeper pipeline: cell → attest → launch → collect → gauge → promote.
Governed by Claude's 3 Laws. Design frozen 4 Aug 2026.

**State:** actively building. Job 2 (packet test) cleared 11 Aug 2026 — all six
packets matched ground truth. Jobs 1, 3, 4, 6 next.

Marrow dossier: `project_blacksmith`. No single README here — start with
`PROMPT_VERIFY_PASTE.md` (the anchor-quote verify job) and `KERNEL_bound.md`/
`KERNEL_execute.md` for the pipeline core.
